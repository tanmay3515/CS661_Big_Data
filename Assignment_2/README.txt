You can simply open the Assignment2.ipynb file in Jupyter notebook and run all the cells.
If you are using anaconda, update some of the libraries of conda to match with the versions of plotly.

You can use the following commands to update libraries
conda update ipython
conda install -c plotly plotly
conda install -c condo-forge ipywidgets